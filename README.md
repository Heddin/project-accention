# Project Ascention (WebGL build)

`Yoohoo!`

#### Now i have minimum viable product (MVP) of my Unity game, called *Project Ascention*

## *Controls*:
#### `WASD` or `Arrows` to *Move*;
#### `Space` to *Jump*;
